import { ContentShortnerPipe } from './content-shortner.pipe';

describe('ContentShortnerPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentShortnerPipe();
    expect(pipe).toBeTruthy();
  });
});
